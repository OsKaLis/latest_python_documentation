from enum import Enum


class headers(Enum):
    whats_new = [('Ссылка на статью', 'Заголовок', 'Редактор, Автор')]
    latest_versions = [('Ссылка на документацию', 'Версия', 'Статус')]
    conclusion_list = [('Статус', 'Количество')]
